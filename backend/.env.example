# Local development (Postgres running locally)
DATABASE_URL=postgresql+asyncpg://pick4me:pick4me@127.0.0.1:5432/pick4me
DATABASE_SYNC_URL=postgresql+psycopg2://pick4me:pick4me@127.0.0.1:5432/pick4me
APP_ENV=development

# Production (Neon) — see .env.production (gitignored)
# DATABASE_URL=postgresql+asyncpg://<user>:<password>@<pooled-host>/neondb?ssl=require
# DATABASE_SYNC_URL=postgresql+psycopg2://<user>:<password>@<direct-host>/neondb?sslmode=require
# APP_ENV=production
