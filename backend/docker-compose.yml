services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: pick4me
      POSTGRES_USER: pick4me
      POSTGRES_PASSWORD: pick4me
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
